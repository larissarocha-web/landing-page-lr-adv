import React from 'react'
import ReactDOM from 'react-dom/client'

import PensaoApp from './PensaoApp.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PensaoApp />
  </React.StrictMode>,
)
